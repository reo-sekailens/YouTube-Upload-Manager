# OAuth clients are configured by importing an operator-owned Google Desktop
# OAuth JSON in the signed application. Do not store OAuth JSON, refresh tokens,
# generated key material, or production signing credentials in environment files.

# Development-only application configuration.
APP_LOG_LEVEL=info
