# Installed-app OAuth configuration. Client IDs are public identifiers, not secrets.
# Configure platform-specific clients in the Google Cloud project; do not commit
# refresh tokens, generated key material, or production signing credentials.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_REDIRECT_SCHEME=

# Development-only application configuration.
APP_LOG_LEVEL=info
